#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"

# Include the Pods-Runner configuration
# If this is a development build, include the development configuration
# For release builds, include the release configuration
# You can change the profile as needed (debug, release, etc.)
# Include the Pods configuration
# Make sure the path is correct based on your project directory
# This file is typically located in "ios/Pods/Target Support Files/Pods-Runner/"
# Don't forget to adjust the paths if your project is structured differently

# For example:
# Debug configuration:
# #include "Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
# Release configuration:
# #include "Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"

# If you want the default debug configuration, use:
# include "Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
# For release, use:
# include "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
